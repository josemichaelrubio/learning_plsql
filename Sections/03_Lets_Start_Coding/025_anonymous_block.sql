/*
Blocks:
    DECLARE (OPTIONAL)
        -- only time to declare variables
    BEGIN (MANDATORY)
        -- code
    EXCEPTION (OPTIONAL)
        -- error handling
    END; (MANDATORY)
An anonymous block is a block of code that is not stored in the database and is not stored in a package.
*/

DECLARE
-- only time to declare variables
BEGIN
    NULL;
END;